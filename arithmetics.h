// heathers:


// this is a kind of dictionary, makes sure that all gets written well, and ensures the pre-procss knows what everything has
#pragma once // makes sure that all include gets imported once
int sum(int, int);
int subtract(int, int);
int multiply(int, int);
int devide(int, int);

void duplicateExistingValues(int *);